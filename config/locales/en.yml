en:
  confirm: Are you sure?
  save: Save
  save_another: Save and add another
  session:
    fail: Login failed
  user:
    create:
      success: Create user successfully
      fail: Create user failed
    update:
      success: Update user successfully
      fail: Update user failed
      at_least_1_admin: Must have at least one admin
    destroy:
      success: Delete user successfully
      fail: Delete user failed
  sidebar:
    area: Area
    device: Device
    group: Group
    user: User
    title_user: Management users
    title_group: Assign device to the area
